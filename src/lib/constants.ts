// =============================================================================
// Layout Constants
// =============================================================================

/** Sidebar panel width in pixels */
export const SIDEBAR_WIDTH = 272;

/** Breakpoint below which the app uses mobile layout (matches Tailwind `md:`) */
export const MOBILE_BREAKPOINT = 768;

// =============================================================================
// Database View Constants
// =============================================================================

/** Width of each column in Board (Kanban) view, in pixels */
export const BOARD_CARD_WIDTH = 280;
